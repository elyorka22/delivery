# Подключение проекта к GitHub - Пошаговая инструкция

## Шаг 1: Создать репозиторий на GitHub

### 1.1. Войти на GitHub

1. Откройте браузер и зайдите на https://github.com
2. Войдите в свой аккаунт (или создайте новый, если нет)

### 1.2. Создать новый репозиторий

1. Нажмите на **"+"** в правом верхнем углу
2. Выберите **"New repository"**

3. Заполните форму:
   - **Repository name:** `fooddelivery` (или любое другое имя)
   - **Description:** `Food Delivery System - Full Stack Application` (опционально)
   - **Visibility:** 
     - ✅ **Public** (если хотите, чтобы код был виден всем)
     - ✅ **Private** (если хотите скрыть код) - **РЕКОМЕНДУЕТСЯ**
   - ❌ **НЕ** ставьте галочки:
     - "Add a README file" (у нас уже есть README)
     - "Add .gitignore" (у нас уже есть .gitignore)
     - "Choose a license" (можно добавить позже)

4. Нажмите **"Create repository"**

### 1.3. Скопировать URL репозитория

После создания репозитория GitHub покажет страницу с инструкциями.

**Скопируйте URL репозитория:**
- Если выбрали **HTTPS:** `https://github.com/ваш-username/fooddelivery.git`
- Если выбрали **SSH:** `git@github.com:ваш-username/fooddelivery.git`

**Рекомендуется использовать HTTPS** (проще для начала)

---

## Шаг 2: Подготовить локальный проект

### 2.1. Проверить текущий статус

В терминале выполните:

```bash
cd /Users/admin/fooddelivery
git status
```

Вы увидите список измененных файлов.

### 2.2. Добавить все файлы в staging

```bash
git add .
```

Это добавит все файлы (кроме тех, что в .gitignore) в staging area.

### 2.3. Создать commit

```bash
git commit -m "Initial commit: Food delivery system ready for deployment"
```

Или более подробное сообщение:

```bash
git commit -m "Initial commit

- Full stack food delivery application
- Frontend: React + TypeScript + Vite
- Backend: Node.js + Express + TypeScript
- Database: Supabase (PostgreSQL)
- Features: Multi-role system (Customer, Admin, Manager, Cook, Courier)
- Ready for deployment"
```

---

## Шаг 3: Подключить GitHub репозиторий

### 3.1. Добавить remote

```bash
git remote add origin https://github.com/ваш-username/fooddelivery.git
```

**Замените `ваш-username` на ваш GitHub username!**

Например:
```bash
git remote add origin https://github.com/johnsmith/fooddelivery.git
```

### 3.2. Проверить, что remote добавлен

```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/ваш-username/fooddelivery.git (fetch)
origin  https://github.com/ваш-username/fooddelivery.git (push)
```

---

## Шаг 4: Запушить код на GitHub

### 4.1. Переименовать ветку в main (если нужно)

```bash
git branch -M main
```

### 4.2. Запушить код

```bash
git push -u origin main
```

**При первом push GitHub попросит авторизацию:**

#### Вариант 1: Personal Access Token (РЕКОМЕНДУЕТСЯ)

1. GitHub попросит username и password
2. **Username:** ваш GitHub username
3. **Password:** НЕ ваш обычный пароль! Нужен Personal Access Token

**Как создать Personal Access Token:**

1. Зайдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token (classic)"
3. Заполните:
   - **Note:** `Food Delivery Project`
   - **Expiration:** выберите срок (например, 90 дней)
   - **Scopes:** отметьте `repo` (полный доступ к репозиториям)
4. Нажмите "Generate token"
5. **ВАЖНО:** Скопируйте токен сразу (он показывается только один раз!)
6. Используйте этот токен как пароль при push

#### Вариант 2: GitHub CLI (альтернатива)

Если установлен GitHub CLI:
```bash
gh auth login
git push -u origin main
```

---

## Шаг 5: Проверить результат

### 5.1. Обновить страницу на GitHub

Зайдите на страницу вашего репозитория:
`https://github.com/ваш-username/fooddelivery`

Вы должны увидеть все файлы проекта!

### 5.2. Проверить в терминале

```bash
git log --oneline
```

Должен показать ваш commit.

---

## ✅ Готово!

Теперь ваш код на GitHub и готов к деплою на Railway и Vercel.

---

## 🔄 Обновление кода в будущем

После любых изменений в проекте:

```bash
# 1. Проверить изменения
git status

# 2. Добавить изменения
git add .

# 3. Создать commit
git commit -m "Описание изменений"

# 4. Запушить на GitHub
git push
```

---

## ⚠️ Важные моменты

### .gitignore

Убедитесь, что `.gitignore` содержит:
- `.env` - файлы с секретами НЕ должны попадать в GitHub!
- `node_modules/` - зависимости
- `dist/` - скомпилированные файлы

### Секреты

**НИКОГДА не коммитьте:**
- `.env` файлы
- Пароли
- API ключи
- JWT секреты

Они уже в `.gitignore`, но проверьте перед commit!

---

## 🆘 Troubleshooting

### Ошибка: "remote origin already exists"

Если remote уже существует, удалите и добавьте заново:

```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/fooddelivery.git
```

### Ошибка: "Authentication failed"

1. Проверьте, что используете Personal Access Token, а не пароль
2. Убедитесь, что токен имеет права `repo`
3. Попробуйте создать новый токен

### Ошибка: "Permission denied"

1. Проверьте, что репозиторий существует на GitHub
2. Убедитесь, что используете правильный URL
3. Проверьте права доступа к репозиторию

---

## 📚 Следующие шаги

После успешного push на GitHub:

1. ✅ Деплой Backend на Railway (см. `RAILWAY_DEPLOY.md`)
2. ✅ Деплой Frontend на Vercel (см. `VERCEL_DEPLOY.md`)



