# Деплой монorepo (Frontend + Backend в одном репозитории)

## ✅ Да, это правильный подход!

Загрузка и фронтенда, и бекенда в один GitHub репозиторий - это **нормальная практика**, называемая **монorepo** (monorepository).

**Преимущества:**
- ✅ Один репозиторий для всего проекта
- ✅ Проще управлять версиями
- ✅ Легче синхронизировать изменения
- ✅ Многие компании используют этот подход (Google, Facebook, Microsoft)

---

## 🎯 Как это работает на Railway и Vercel

### Railway (Backend)

Railway позволяет создать **несколько сервисов** из одного репозитория:

1. **Первый сервис (Backend):**
   - Root Directory: `backend`
   - Start Command: `npm start`
   - Railway будет использовать только папку `backend/`

2. **Второй сервис (Frontend) - опционально:**
   - Можно также задеплоить frontend на Railway
   - Root Directory: `frontend`
   - Но **рекомендуется использовать Vercel для frontend** (бесплатно и лучше для React)

### Vercel (Frontend)

Vercel также работает с монoreпо:

1. **Настройка проекта:**
   - Root Directory: `frontend`
   - Vercel будет использовать только папку `frontend/`
   - Остальные файлы (backend) будут проигнорированы

---

## 📋 Пошаговая инструкция для Railway

### Шаг 1: Создать первый сервис (Backend)

1. Зайдите на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите репозиторий `elyorka22/delivery`
5. Railway создаст **первый сервис**

### Шаг 2: Настроить Backend сервис

1. Откройте созданный сервис
2. Перейдите в **Settings**
3. Найдите секцию **"Root Directory"**
4. Установите: `backend`
5. Найдите **"Start Command"**
6. Установите: `npm start` (Railway автоматически запустит `npm run build`)

**Важно:** Railway будет использовать **только папку `backend/`**, игнорируя `frontend/` и другие файлы.

### Шаг 3: Добавить переменные окружения

В настройках сервиса → **Variables**:

```env
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
JWT_SECRET=[СГЕНЕРИРОВАННЫЙ_КЛЮЧ]
FRONTEND_URL=https://yourproject.vercel.app
NODE_ENV=production
```

### Шаг 4: Получить URL Backend

1. В настройках → **Networking**
2. Нажмите "Generate Domain"
3. Скопируйте URL (например: `https://yourproject.up.railway.app`)

---

## 📋 Пошаговая инструкция для Vercel

### Шаг 1: Создать проект

1. Зайдите на https://vercel.com
2. Нажмите "Add New..." → "Project"
3. Выберите репозиторий `elyorka22/delivery`

### Шаг 2: Настроить Frontend

Vercel автоматически определит, что это Vite проект, но нужно указать:

1. **Root Directory:** `frontend`
   - Нажмите "Edit" рядом с Root Directory
   - Введите: `frontend`
   - Нажмите "Save"

2. **Build Command:** `npm run build` (автоматически)
3. **Output Directory:** `dist` (автоматически)

**Важно:** Vercel будет использовать **только папку `frontend/`**, игнорируя `backend/` и другие файлы.

### Шаг 3: Добавить переменные окружения

В настройках проекта → **Environment Variables**:

```env
VITE_API_URL=https://yourproject.up.railway.app/api
```

**Важно:** Добавьте для всех окружений (Production, Preview, Development)

---

## 🎯 Визуализация структуры

```
elyorka22/delivery (GitHub репозиторий)
│
├── backend/          ← Railway использует ТОЛЬКО это
│   ├── src/
│   ├── package.json
│   └── ...
│
├── frontend/         ← Vercel использует ТОЛЬКО это
│   ├── src/
│   ├── package.json
│   └── ...
│
├── README.md
└── ...
```

**Railway:**
- Видит только `backend/`
- Запускает `cd backend && npm install && npm run build && npm start`

**Vercel:**
- Видит только `frontend/`
- Запускает `cd frontend && npm install && npm run build`
- Деплоит папку `dist/`

---

## ✅ Проверка правильности настройки

### Railway (Backend)

После деплоя проверьте логи:
- Должны видеть: `Server is running on port 5000` (или другой порт)
- НЕ должно быть ошибок про `frontend/`

### Vercel (Frontend)

После деплоя проверьте:
- Сайт загружается
- API запросы работают (откройте DevTools → Network)
- НЕ должно быть ошибок про `backend/`

---

## 🔄 Обновление кода

После любых изменений:

```bash
# 1. Закоммитить изменения
git add .
git commit -m "Описание изменений"

# 2. Запушить на GitHub
git push

# 3. Railway и Vercel автоматически обнаружат изменения и задеплоят
```

**Railway:**
- Автоматически передеплоит backend при push в `backend/`

**Vercel:**
- Автоматически передеплоит frontend при push в `frontend/`

---

## ⚠️ Важные моменты

### 1. Root Directory обязателен!

Без указания Root Directory:
- Railway попытается найти `package.json` в корне репозитория
- Это приведет к ошибке, так как в корне нет `package.json`

### 2. Переменные окружения

- **Railway:** Использует переменные из Settings → Variables
- **Vercel:** Использует переменные из Settings → Environment Variables

### 3. Игнорирование файлов

`.gitignore` работает для всего репозитория:
- `.env` файлы не попадут ни в backend, ни в frontend
- `node_modules/` не попадут в репозиторий

---

## 🆘 Troubleshooting

### Проблема: Railway не находит package.json

**Решение:** Убедитесь, что Root Directory установлен на `backend`

### Проблема: Vercel не находит package.json

**Решение:** Убедитесь, что Root Directory установлен на `frontend`

### Проблема: Ошибка "Cannot find module"

**Решение:** 
1. Проверьте Root Directory
2. Убедитесь, что `package.json` находится в правильной папке
3. Проверьте логи деплоя

---

## 📚 Альтернативный подход (не рекомендуется)

Можно разделить на два репозитория:
- `elyorka22/delivery-backend`
- `elyorka22/delivery-frontend`

**Но это усложняет:**
- Управление версиями
- Синхронизацию изменений
- Деплой (нужно настраивать два репозитория)

**Монorepo - лучший выбор для вашего проекта!** ✅

---

## ✅ Резюме

1. ✅ **Монorepo - правильный подход** для вашего проекта
2. ✅ **Railway** деплоит только `backend/` (благодаря Root Directory)
3. ✅ **Vercel** деплоит только `frontend/` (благодаря Root Directory)
4. ✅ Они **не мешают друг другу**
5. ✅ Каждый сервис видит **только свою папку**

**Можете смело деплоить!** 🚀



