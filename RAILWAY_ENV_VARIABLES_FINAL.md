# Переменные окружения для Railway

## ✅ Обязательные переменные (НЕ УДАЛЯТЬ)

### 1. JWT_SECRET
```
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
```
**Зачем:** Для подписи и проверки JWT токенов при аутентификации.
**Как создать:** Используйте длинную случайную строку (минимум 32 символа).
**Пример генерации:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 2. NODE_ENV
```
NODE_ENV=production
```
**Зачем:** Определяет окружение приложения (development/production).
**Значения:** `production` или `development`

### 3. FRONTEND_URL
```
FRONTEND_URL=https://yourproject.vercel.app
```
**Зачем:** URL фронтенда для CORS и Socket.IO.

### 4. FIREBASE_SERVICE_ACCOUNT
```
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
```
**Зачем:** Учетные данные Firebase для подключения к Firestore.
**Формат:** JSON строка (весь файл service-account.json в одну строку).

### 5. PORT (опционально)
```
PORT=8080
```
**Зачем:** Порт для сервера. Railway обычно устанавливает автоматически.

---

## ❌ Можно удалить (после миграции на Firebase)

### DATABASE_URL
```
DATABASE_URL=postgresql://...
```
**Статус:** Больше не нужна после полной миграции на Firebase.
**Действие:** Можно удалить из Railway после того, как все контроллеры переписаны на Firebase.

---

## 📋 Итоговый список переменных для Railway

После миграции на Firebase вам нужны только:

1. ✅ `JWT_SECRET` - обязательно
2. ✅ `NODE_ENV=production` - обязательно
3. ✅ `FRONTEND_URL` - обязательно
4. ✅ `FIREBASE_SERVICE_ACCOUNT` - обязательно
5. ❌ `DATABASE_URL` - можно удалить (после миграции)

---

## 🔧 Как добавить/изменить переменные в Railway

1. Откройте ваш проект в Railway
2. Перейдите в **Variables** (вкладка слева)
3. Нажмите **+ New Variable**
4. Введите имя переменной и значение
5. Нажмите **Add**
6. Railway автоматически перезапустит сервис

---

## ⚠️ Важно

- **JWT_SECRET** должен быть уникальным и секретным
- **FIREBASE_SERVICE_ACCOUNT** должен быть полным JSON объектом в одной строке
- После изменения переменных подождите перезапуска сервиса (обычно 1-2 минуты)

