# ✅ Backend успешно задеплоен на Railway!

## Статус

```
Server is running on port 8080
```

Сервер работает! 🎉

---

## Следующие шаги

### 1. Получить URL Backend

1. В Railway dashboard откройте ваш проект
2. Откройте сервис (service)
3. Перейдите в **Settings** → **Networking**
4. Нажмите **"Generate Domain"** (если еще не создан)
5. Скопируйте URL (например: `https://yourproject.up.railway.app`)

### 2. Проверить работу Backend

Откройте в браузере:
```
https://yourproject.up.railway.app/api/health
```

Должен вернуться:
```json
{
  "status": "ok",
  "message": "Server is running"
}
```

### 3. Добавить переменные окружения (если еще не добавлены)

В Railway → Settings → Variables добавьте:

```env
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
JWT_SECRET=[СГЕНЕРИРОВАННЫЙ_КЛЮЧ]
FRONTEND_URL=https://yourproject.vercel.app
NODE_ENV=production
```

**Как сгенерировать JWT_SECRET:**
```bash
openssl rand -base64 32
```

**Важно:** После добавления переменных окружения Railway автоматически перезапустит сервер.

### 4. Проверить подключение к базе данных

После добавления `DATABASE_URL` проверьте логи:
- Не должно быть ошибок подключения к базе данных
- Если есть ошибки, проверьте правильность `DATABASE_URL`

---

## Следующий шаг: Деплой Frontend на Vercel

Теперь нужно задеплоить frontend:

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New..." → "Project"
4. Выберите репозиторий `elyorka22/delivery`
5. Настройте:
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build` (автоматически)
   - **Output Directory:** `dist` (автоматически)
6. Добавьте переменную окружения:
   - `VITE_API_URL` = `https://yourproject.up.railway.app/api`
7. Нажмите "Deploy"

Подробная инструкция: `VERCEL_DEPLOY.md`

---

## Проверка работы

### Backend API endpoints

После добавления переменных окружения проверьте:

1. **Health check:**
   ```
   GET https://yourproject.up.railway.app/api/health
   ```

2. **Регистрация (если нужно):**
   ```
   POST https://yourproject.up.railway.app/api/auth/register
   ```

3. **Авторизация:**
   ```
   POST https://yourproject.up.railway.app/api/auth/login
   ```

---

## Мониторинг

### Логи

В Railway можно просматривать логи в реальном времени:
1. Откройте сервис
2. Вкладка **Deployments** → выберите последний деплой
3. Вкладка **Logs**

### Метрики

Railway предоставляет:
- CPU использование
- Memory использование
- Network трафик
- Request count

---

## Troubleshooting

### Проблема: API не отвечает

1. Проверьте, что сервер запущен (логи показывают "Server is running")
2. Проверьте URL (должен быть `https://`, не `http://`)
3. Проверьте переменные окружения

### Проблема: Ошибка подключения к базе данных

1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что Supabase проект активен
3. Проверьте пароль (может содержать специальные символы - нужно URL encode)

### Проблема: CORS ошибки

1. Убедитесь, что `FRONTEND_URL` установлен правильно
2. После деплоя frontend обновите `FRONTEND_URL` на URL Vercel

---

## Готово! 🎉

Backend успешно задеплоен! Теперь можно:
1. ✅ Получить URL backend
2. ✅ Добавить переменные окружения
3. ✅ Задеплоить frontend на Vercel
4. ✅ Обновить `FRONTEND_URL` в Railway
5. ✅ Обновить `VITE_API_URL` в Vercel



