# Когда и как подключать Supabase

## 🤔 Когда подключать Supabase?

### Вариант 1: Подключить СЕЙЧАС (для разработки)
**Плюсы:**
- ✅ Работаете с реальной базой данных с самого начала
- ✅ Легче тестировать и отлаживать
- ✅ Данные сохраняются в облаке (не потеряете при перезапуске)
- ✅ Бесплатный тариф Supabase достаточен для разработки

**Минусы:**
- ⚠️ Нужен интернет для работы
- ⚠️ Немного медленнее, чем локальная SQLite

### Вариант 2: Подключить ПОСЛЕ деплоя (только для продакшена)
**Плюсы:**
- ✅ Быстрая разработка на локальной SQLite
- ✅ Не нужен интернет для разработки

**Минусы:**
- ⚠️ Придется мигрировать данные позже
- ⚠️ Могут быть различия между разработкой и продакшеном

## 💡 Рекомендация

**Подключите Supabase СЕЙЧАС** - это проще и безопаснее. Бесплатный тариф Supabase дает:
- 500 MB базы данных
- 2 GB bandwidth
- Достаточно для разработки и небольших проектов

---

## 📋 Пошаговая инструкция подключения Supabase

### ШАГ 1: Создать аккаунт в Supabase (2 минуты)

1. Откройте https://supabase.com
2. Нажмите **"Start your project"** или **"Sign in"**
3. Войдите через GitHub, Google или создайте аккаунт по email
4. Подтвердите email (если нужно)

### ШАГ 2: Создать новый проект (3-5 минут)

1. После входа нажмите **"New Project"** (зеленая кнопка)
2. Заполните форму:
   
   **Organization:**
   - Если первый раз - создастся автоматически
   - Или выберите существующую
   
   **Project Name:**
   - Например: `fooddelivery` или `my-food-app`
   - Это только для вас, не влияет на URL
   
   **Database Password:**
   - ⚠️ **ВАЖНО!** Придумайте надежный пароль
   - ⚠️ **СОХРАНИТЕ ЕГО!** Он понадобится для подключения
   - Минимум 8 символов
   - Пример: `MySecurePass123!`
   
   **Region:**
   - Выберите ближайший регион
   - Для Узбекистана: `Southeast Asia (Singapore)` или `West US`
   
   **Pricing Plan:**
   - Выберите **Free** (бесплатный тариф)
   
3. Нажмите **"Create new project"**
4. Подождите 2-3 минуты пока создается проект
   - Появится прогресс-бар
   - Не закрывайте страницу!

### ШАГ 3: Получить Connection String (1 минута)

После создания проекта:

1. В левом меню нажмите на **шестеренку** ⚙️ (Settings)
2. Выберите **"Database"**
3. Прокрутите вниз до секции **"Connection string"**
4. Выберите вкладку **"URI"** (не "Session mode" или "Transaction")
5. Скопируйте строку подключения
   
   Она будет выглядеть так:
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
   ```
   
   Или так:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

6. **ВАЖНО:** Замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта

### ШАГ 4: Настроить .env файл (2 минуты)

1. Откройте терминал в папке проекта:
   ```bash
   cd /Users/admin/fooddelivery/backend
   ```

2. Создайте файл `.env` из примера:
   ```bash
   cp .env.example .env
   ```

3. Откройте файл `.env` в редакторе:
   ```bash
   # В VS Code или другом редакторе
   code .env
   # Или
   nano .env
   ```

4. Вставьте ваш Connection String:
   ```env
   DATABASE_URL="postgresql://postgres:[ВАШ-ПАРОЛЬ]@db.xxxxx.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"
   ```
   
   **Пример:**
   ```env
   DATABASE_URL="postgresql://postgres:MySecurePass123!@db.abcdefgh.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"
   ```
   
   **Важно:**
   - Замените `[ВАШ-ПАРОЛЬ]` на реальный пароль
   - Добавьте `?pgbouncer=true&connection_limit=1` в конец URL
   - Это нужно для правильной работы с Supabase

5. Настройте остальные переменные:
   ```env
   JWT_SECRET="your-secret-key-change-in-production-12345"
   PORT=5000
   FRONTEND_URL="http://localhost:3000"
   ```

### ШАГ 5: Применить миграции базы данных (1 минута)

1. Сгенерируйте Prisma Client:
   ```bash
   cd backend
   npx prisma generate
   ```

2. Примените миграции к Supabase:
   ```bash
   npx prisma migrate deploy
   ```
   
   Это создаст все таблицы в вашей Supabase базе данных.

### ШАГ 6: Проверить подключение (1 минута)

1. Откройте Prisma Studio:
   ```bash
   npx prisma studio
   ```
   
2. Должен открыться браузер с интерфейсом базы данных
3. Вы должны увидеть все таблицы: `users`, `restaurants`, `menu_items`, `orders` и т.д.

### ШАГ 7: Создать тестовых пользователей (1 минута)

```bash
npm run create-test-users
```

Это создаст:
- Супер-админа
- Менеджера
- Повара
- Курьера
- Клиента

### ШАГ 8: Запустить сервер

```bash
npm run dev
```

Сервер должен запуститься без ошибок!

---

## ✅ Проверка что все работает

1. **Backend запущен:**
   ```bash
   curl http://localhost:5000/api/health
   ```
   Должен вернуть: `{"status":"ok","message":"Server is running"}`

2. **База данных подключена:**
   - Откройте Prisma Studio: `npx prisma studio`
   - Должны быть видны таблицы

3. **API работает:**
   - Откройте http://localhost:3000
   - Попробуйте зарегистрироваться или войти

---

## 🔍 Где найти Connection String в Supabase?

Если забыли где найти:

1. Зайдите на https://supabase.com/dashboard
2. Выберите ваш проект
3. В левом меню: **Settings** (шестеренка) → **Database**
4. Прокрутите до **"Connection string"**
5. Вкладка **"URI"**

---

## ⚠️ Важные моменты

### Безопасность пароля
- Никогда не коммитьте `.env` файл в Git!
- Файл `.env` уже должен быть в `.gitignore`

### Если забыли пароль
- В Supabase Dashboard: **Settings** → **Database** → **Database password**
- Можно сбросить пароль (но это перезапустит базу данных)

### Два типа Connection String в Supabase

1. **URI (для приложений)** - используйте этот:
   ```
   postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

2. **Session mode / Transaction mode** - для других целей

---

## 🚀 Что дальше?

После подключения Supabase:

1. ✅ Разрабатывайте как обычно
2. ✅ Все данные сохраняются в облаке
3. ✅ При деплое на сервер используйте тот же Connection String
4. ✅ Или создайте отдельный проект Supabase для продакшена

---

## 📞 Если что-то не работает

### Ошибка подключения
- Проверьте правильность пароля в `DATABASE_URL`
- Убедитесь, что добавили `?pgbouncer=true&connection_limit=1`
- Проверьте, что проект Supabase активен (не приостановлен)

### Ошибка миграции
- Убедитесь, что выполнили `npx prisma generate`
- Проверьте права доступа к базе данных

### Не видно таблиц в Prisma Studio
- Проверьте, что миграции применены: `npx prisma migrate deploy`
- Обновите Prisma Client: `npx prisma generate`

---

## 🎯 Итог

**Подключите Supabase СЕЙЧАС** - это займет 10-15 минут, но сэкономит время позже!

После подключения:
- ✅ Работаете с реальной базой данных
- ✅ Данные не потеряются
- ✅ Готово к деплою (просто используйте тот же Connection String)

