# Пошаговое подключение Supabase - ПРЯМО СЕЙЧАС

## ✅ ШАГ 1: Создать аккаунт в Supabase

1. Откройте браузер и перейдите на: **https://supabase.com**

2. Нажмите кнопку **"Start your project"** (зеленая кнопка справа вверху)

3. Выберите способ входа:
   - **GitHub** (рекомендуется, если есть аккаунт)
   - **Google**
   - Или создайте аккаунт по email

4. Если создаете по email:
   - Введите email и пароль
   - Подтвердите email (проверьте почту)

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после создания аккаунта переходите к шагу 2.

---

## ✅ ШАГ 2: Создать новый проект

1. После входа вы увидите Dashboard
2. Нажмите большую зеленую кнопку **"New Project"**

3. Заполните форму:

   **Organization:**
   - Если первый раз - выберите "Create new organization"
   - Или выберите существующую

   **Project Name:**
   - Введите: `fooddelivery` (или любое другое имя)
   - Это только для вас, не влияет на работу

   **Database Password:**
   - ⚠️ **ОЧЕНЬ ВАЖНО!** Придумайте надежный пароль
   - ⚠️ **ОБЯЗАТЕЛЬНО СОХРАНИТЕ ЕГО!** Запишите в безопасное место
   - Минимум 8 символов
   - Пример хорошего пароля: `FoodDelivery2024!Secure`
   - Этот пароль понадобится для подключения!

   **Region:**
   - Выберите ближайший регион:
     - Для Узбекистана: `Southeast Asia (Singapore)` 
     - Или `West US (California)`

   **Pricing Plan:**
   - Выберите **Free** (бесплатный тариф)

4. Нажмите **"Create new project"**

5. **ПОДОЖДИТЕ 2-3 МИНУТЫ** пока создается проект
   - Появится прогресс-бар
   - Не закрывайте страницу!
   - Когда увидите "Project is ready" - можно продолжать

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после создания проекта переходите к шагу 3.

---

## ✅ ШАГ 3: Получить Connection String

1. После создания проекта вы попадете в Dashboard проекта

2. В левом меню найдите и нажмите на **шестеренку** ⚙️ (Settings)

3. В меню Settings выберите **"Database"**

4. Прокрутите страницу вниз до секции **"Connection string"**

5. Вы увидите несколько вкладок:
   - **URI** ← ВЫБЕРИТЕ ЭТУ!
   - Session mode
   - Transaction mode

6. Нажмите на вкладку **"URI"**

7. Вы увидите строку вида:
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
   ```
   
   Или:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

8. **СКОПИРУЙТЕ ЭТУ СТРОКУ** (Ctrl+C или Cmd+C)

9. **ВАЖНО:** В скопированной строке замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта

   Например, если пароль был `FoodDelivery2024!Secure`, строка должна быть:
   ```
   postgresql://postgres:FoodDelivery2024!Secure@db.xxxxx.supabase.co:5432/postgres
   ```

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после получения Connection String переходите к шагу 4.

---

## ✅ ШАГ 4: Настроить .env файл

1. Откройте терминал в папке проекта:
   ```bash
   cd /Users/admin/fooddelivery/backend
   ```

2. Создайте файл `.env` из примера:
   ```bash
   cp .env.example .env
   ```

3. Откройте файл `.env` в редакторе:
   ```bash
   # Если используете VS Code:
   code .env
   
   # Или используйте nano:
   nano .env
   
   # Или откройте в любом текстовом редакторе
   ```

4. В файле `.env` найдите строку:
   ```env
   DATABASE_URL="postgresql://postgres:password@localhost:5432/fooddelivery?schema=public"
   ```

5. **ЗАМЕНИТЕ** эту строку на ваш Connection String из шага 3, но **ДОБАВЬТЕ** в конец `?pgbouncer=true&connection_limit=1`

   **Пример:**
   ```env
   DATABASE_URL="postgresql://postgres:FoodDelivery2024!Secure@db.xxxxx.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1"
   ```

   **Важно:**
   - Замените `[YOUR-PASSWORD]` на ваш реальный пароль
   - Добавьте `?pgbouncer=true&connection_limit=1` в конец
   - Оставьте кавычки вокруг строки

6. Убедитесь, что остальные переменные настроены:
   ```env
   JWT_SECRET="your-secret-key-change-in-production-12345"
   PORT=5000
   FRONTEND_URL="http://localhost:3000"
   ```

7. Сохраните файл (Ctrl+S или Cmd+S)

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после настройки .env переходите к шагу 5.

---

## ✅ ШАГ 5: Применить миграции

1. Убедитесь, что вы в папке backend:
   ```bash
   cd /Users/admin/fooddelivery/backend
   ```

2. Сгенерируйте Prisma Client:
   ```bash
   npx prisma generate
   ```
   
   Это займет 10-20 секунд. Должно появиться: "Generated Prisma Client"

3. Примените миграции к Supabase:
   ```bash
   npx prisma migrate deploy
   ```
   
   Это создаст все таблицы в вашей Supabase базе данных.
   
   Должно появиться что-то вроде:
   ```
   Applying migration `20260104190910_init`
   The following migration(s) have been applied:
   - 20260104190910_init
   ```

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после миграций переходите к шагу 6.

---

## ✅ ШАГ 6: Проверить подключение

1. Откройте Prisma Studio для просмотра базы данных:
   ```bash
   npx prisma studio
   ```
   
   Это откроет браузер с адресом `http://localhost:5555`

2. В браузере вы должны увидеть список таблиц:
   - `users`
   - `restaurants`
   - `menu_items`
   - `orders`
   - `order_items`
   - `order_status_history`
   - `courier_locations`

3. Если видите таблицы - **ВСЕ РАБОТАЕТ!** ✅

4. Закройте Prisma Studio (Ctrl+C в терминале)

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после проверки переходите к шагу 7.

---

## ✅ ШАГ 7: Создать тестовых пользователей

1. В терминале выполните:
   ```bash
   npm run create-test-users
   ```

2. Должно появиться:
   ```
   Test users created successfully!
   ```

3. Проверьте в Prisma Studio (если еще открыт) или откройте снова:
   ```bash
   npx prisma studio
   ```
   
   Перейдите в таблицу `users` - должны быть созданы пользователи.

**⏸️ ОСТАНОВИТЕСЬ ЗДЕСЬ** - после создания пользователей переходите к шагу 8.

---

## ✅ ШАГ 8: Запустить сервер

1. Запустите backend сервер:
   ```bash
   npm run dev
   ```

2. Должно появиться:
   ```
   Server is running on port 5000
   ```

3. Если видите это сообщение - **ВСЕ ПОДКЛЮЧЕНО!** ✅

4. Откройте новый терминал и проверьте API:
   ```bash
   curl http://localhost:5000/api/health
   ```
   
   Должно вернуть: `{"status":"ok","message":"Server is running"}`

---

## 🎉 ГОТОВО!

Теперь ваш проект подключен к Supabase!

### Что дальше?

1. Запустите frontend (в новом терминале):
   ```bash
   cd /Users/admin/fooddelivery/frontend
   npm run dev
   ```

2. Откройте http://localhost:3000 в браузере

3. Попробуйте зарегистрироваться или войти с тестовыми данными

---

## ❌ Если что-то пошло не так

### Ошибка подключения к базе данных

**Проблема:** `Can't reach database server`

**Решение:**
1. Проверьте правильность пароля в `DATABASE_URL`
2. Убедитесь, что добавили `?pgbouncer=true&connection_limit=1`
3. Проверьте, что проект Supabase активен (не приостановлен)

### Ошибка миграции

**Проблема:** `Migration failed`

**Решение:**
1. Убедитесь, что выполнили `npx prisma generate`
2. Проверьте Connection String в `.env`
3. Попробуйте: `npx prisma migrate reset` (осторожно, удалит данные!)

### Не видно таблиц в Prisma Studio

**Проблема:** Пустая база данных

**Решение:**
1. Проверьте, что миграции применены: `npx prisma migrate deploy`
2. Обновите Prisma Client: `npx prisma generate`

---

## 📝 Важные заметки

1. **Никогда не коммитьте `.env` в Git!** (он уже в .gitignore)

2. **Сохраните пароль от Supabase** - он понадобится для подключения

3. **Connection String** можно найти в любое время:
   - Supabase Dashboard → Settings → Database → Connection string → URI

4. **Бесплатный тариф Supabase:**
   - 500 MB базы данных
   - 2 GB bandwidth
   - Достаточно для разработки

---

## 🚀 Готово к разработке!

Теперь можете разрабатывать как обычно. Все данные сохраняются в Supabase!

